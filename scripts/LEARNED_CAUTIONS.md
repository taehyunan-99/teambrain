# scripts — LEARNED CAUTIONS

<!--
이 파일은 작업 중 발견된 실수/주의사항을 누적하는 자리다.
- `learn` 스킬(`/learn` 또는 Codex의 `$learn`)이 이 파일에만 항목을 추가한다.
- 같은 폴더의 본문 가이드(AGENTS.md / CLAUDE.md)는 절대 수정하지 않는다.
- update 스킬도 이 파일은 자동 덮어쓰지 않는다 — 사용자 결정으로 만들어진 자산이므로 보존.
- 변경이 필요한 경우 반드시 사용자에게 확인을 받고 진행한다.
-->

_(아직 없음)_

<!--
== 항목 형식 ==
- (YYYY-MM-DD) 한 줄 메모. 사용자 표현 그대로 누적하며 윤색하지 않는다.
- 같거나 비슷한 항목이 있으면 추가 전에 사용자에게 알린다.

== 분할 안내 ==
이 파일이 100줄을 넘기 시작하면 토픽별로 분할을 검토한다.
예: 도메인 함정과 운영 함정이 섞이면 `@./learned-domain.md`, `@./learned-ops.md`로 분리.
분할 후 본문 가이드의 LEARNED CAUTIONS 섹션은 새 파일들을 함께 import 한다.
-->
