---
created: 2025-05-14
tags: [pr-review, raw-dump, idempotency, redis]
pr: 61
---

# PR #61 — feat: 멱등성 미들웨어 1차 (redis SETNX)

> 작성: 이준호 / 리뷰: 박서연, 김도현 / 머지: 2025-05-14

**박서연 (idempotency.go:18)**: 키를 클라가 X-Idempotency-Key 헤더로 줄 수도 있고, 없으면 서버가 order_id로 만든다 — 이렇게 두 갈래네요. 굳이 혼합인 이유가?
**이준호**: 지금 일부 가맹점만 헤더를 붙여서요. 클라 강제하면 헤더 안 보내는 쪽이 다 막혀서, 일단 없으면 order_id 기반으로 폴백해두려고요. 헤더 강제는 SDK 깔리고 나서 따로
**박서연**: ㅇㅋ 폴백 키 포맷은 충돌 안 나게 prefix 붙여줘요. order_id 그대로 쓰면 헤더로 들어온 UUID랑 섞일 수 있으니
**이준호**: 넵 `order:` prefix 붙였어요

**박서연 (idempotency.go:34)**: SETNX 성공이면 신규, 실패면 중복으로 보고 진행 중인 요청 막는 거죠? TTL은요
**이준호**: 네 SET key val NX EX 600. 일단 10분 잡았어요. 결제 한 건 처리시간 생각하면 충분하고, 너무 길게 잡으면 같은 주문 정상 재시도까지 막힐까봐
**박서연**: 10분이면 짧긴 한데 1차니까 일단 두죠

**박서연 (idempotency.go:41)**: 여기가 제일 걸리는데 — redis가 늦거나 죽으면 어떻게 돼요? SETNX 호출 자체가 에러나 타임아웃이면
**이준호**: 그땐 통과시켜요. 못 막아도 결제 자체는 돼야 하니까요. 멱등 체크하다가 redis 한 번 삐끗했다고 결제를 통째로 떨구는 게 더 손해라
**박서연**: 음... 통과시키면 그 틈에 같은 키 두 번 들어오면 둘 다 통과하긴 하네요
**이준호**: 네 근데 redis 죽는 빈도 자체가 드무니까. 막는 게 목적이긴 한데 100%는 아니고, 일단 대부분 중복을 거르는 게 1차 목표라서요. redis 정상일 땐 SETNX가 다 잡아줘요
**박서연**: 그건 맞아요. 완벽하게 막으려면 DB쪽에 뭐가 있어야 하는데 지금 그거까지 가면 일정이... 일단 redis로 급한 불 끄는 걸로
**이준호**: ㅋㅋ 넵 급한 불용입니다

**김도현 (idempotency.go:41)**: 에러 통과(fail-open) 동의해요. 결제 못 받는 게 더 큰 손해라 트레이드오프상 맞고, redis 가용성 높으니 그 틈은 드물 거예요. 대신 SETNX 에러/타임아웃 카운터는 메트릭으로 빼주세요. 얼마나 자주 통과되는지는 봐야 하니
**이준호**: 메트릭 추가했어요 `idem_redis_error_total`

**김도현**: 1차 치고 깔끔하네요. approve 👍
**박서연**: approve. TTL이랑 DB 보강은 나중에 다시 보는 걸로 👍
